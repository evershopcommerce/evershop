/**
 * Page builder module bootstrap. Phase 3a doesn't register any
 * processors / hooks / widgets — that surface arrives in Phase 3b/3c.
 *
 * Kept as a placeholder so the module is consistent with other core modules
 * and future hook/processor registrations can land here.
 */
export default function () {
  // intentionally empty
}
