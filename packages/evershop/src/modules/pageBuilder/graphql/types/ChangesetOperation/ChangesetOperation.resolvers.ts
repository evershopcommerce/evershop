// All ChangesetOperation fields are resolved from the camelCased row directly
// (changesetOperationId, uuid, changesetId, route, entityUrn, oldPayload,
// newPayload, changeOrder, createdAt). No custom resolvers needed.
export default {};
