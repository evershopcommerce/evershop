"""
Represents a customer
"""
type Customer {
  customerId: Int!
  uuid: String!
  status: Int!
  email: String!
  fullName: String!
  createdAt: Date!
}

extend type Query {
  currentCustomer: Customer
}
