type Shop {
  homeUrl: String!
  name: String
  description: String
  phone: String
  email: String
  language: String!
  currency: String!
  timezone: String!
  weightUnit: String
}

extend type Query {
  shop: Shop
}