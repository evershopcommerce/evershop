import React from 'react';

export default function Index({ post, category }) {
  return <h1>This is CMS page copy</h1>;
}

export const layout = {
  id: "cmsPagecopy",
  areaId: "content",
  sortOrder: 1
}
